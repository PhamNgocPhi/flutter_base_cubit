import 'package:flutter/material.dart';

class UpcomingRacesPage extends StatelessWidget {
  const UpcomingRacesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("upcoming"),),
    );
  }
}

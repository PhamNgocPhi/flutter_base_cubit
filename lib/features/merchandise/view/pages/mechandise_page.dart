import 'package:flutter/material.dart';

class MerchandisePage extends StatelessWidget {
  const MerchandisePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("MerchandisePage"),),
    );
  }
}
